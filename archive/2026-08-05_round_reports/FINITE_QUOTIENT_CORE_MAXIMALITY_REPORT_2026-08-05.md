# Finite Condition-F quotient core maximality — round 68

## Completed engineering theorem

`derive_finite_quotient_core_maximality` now composes exact evidence over a
complete finite quotient SCC catalogue.  It never accepts a floating Perron
estimate or a user-supplied Boolean dominance claim.

For each non-core quotient SCC it tries, in order:

1. exact terminal-permutation recognition (`rho = 1`);
2. greatest multiplicity-preserving path injection into the core;
3. exact renewal/cone transfer;
4. exact implicit rank-one paired-matrix dominance.

The catalogue closes only when every SCC is covered by one of these mechanisms.
The resulting object feeds directly into
`compose_universal_dominance_from_finite_quotient`.

## Exact arithmetic quotient replay

The coefficient-one arithmetic quotient was rebuilt and replayed with exact
Perron comparison through the first complexity transition.

| n | non-core SCCs | terminal | path injection | renewal transfer | paired fallback | catalogue |
|---:|---:|---:|---:|---:|---:|:---:|
| 3 | 0 | 0 | 0 | 0 | 0 | PASS |
| 4 | 2 | 2 | 0 | 0 | 0 | PASS |
| 5 | 2 | 0 | 2 | 0 | 0 | PASS |
| 6 | 6 | 4 | 0 | 2 | 0 | PASS |
| 7 | 6 | 0 | 3 | 3 | 0 | PASS |

Thus every finite quotient SCC through `n=7` is closed structurally; none
requires the generic paired-matrix fallback and none is justified only by a
numerical estimate.

## Remaining universal theorem

The remaining theorem is no longer spectral comparison for a supplied finite
catalogue.  That operation is complete.  The remaining promotion is the
uniform quotient-family exhaustion theorem:

> Every recurrent SCC generated by the Condition-F branching grammar is either
> terminal permutation, admits a multiplicity-preserving descent, or admits a
> renewal transfer to a lower/base family.

The exact `n=4..7` mechanism pattern is now executable evidence for that
trichotomy.  The next proof should derive the trichotomy from the quotient
state grammar rather than enumerate another absolute coefficient hull.
