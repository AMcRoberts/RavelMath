# Next session: self-observing universal-n proof system

The authoritative next-session checklist is:

```text
../.ravel/NEXT_SESSION_SELF_OBSERVING_UNIVERSAL_N.md
```

Read it before `docs/READINGLIST.NBONACCI_SHOOT_THE_MOON.md` or any older proof-generator handoff. It supersedes instructions to begin by extending the external Lua/C++ proof-text generator wherever those instructions conflict with reflective library instrumentation.

The non-negotiable boundary is: the app triggers ordinary math-library operations; those operations emit semantic proof evidence when Lua enables reflection; a generic supervisor submits focused obligations to Lean and ingests the result. The app must not own the proof.
